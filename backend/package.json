{
  "name": "digital-donor-backend",
  "version": "1.0.0",
  "description": "Microservices backend for Digital Donor",
  "scripts": {
    "dev": "concurrently \"npm run dev:gateway\" \"npm run dev:users\" \"npm run dev:donations\" \"npm run dev:notifications\" \"npm run dev:payments\"",
    "tunnel": "node tunnel.js",
    "dev:gateway": "npm run dev --prefix api-gateway",
    "dev:users": "npm run dev --prefix users-service",
    "dev:donations": "npm run dev --prefix donations-service",
    "dev:notifications": "npm run dev --prefix notifications-service",
    "dev:payments": "npm run dev --prefix payment-service",
    "install:all": "cd api-gateway && npm install && cd ../users-service && npm install && cd ../donations-service && npm install && cd ../notifications-service && npm install && cd ../payment-service && npm install && cd ../shared && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "localtunnel": "^2.0.2"
  }
}
